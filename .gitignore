__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.DS_Store

# Runtime state (never commit account data / audit trail)
journal/
state/
HALT
data/

# Claude Code git worktrees (separate checkouts, not repo content)
.claude/worktrees/

# graphify output — local tooling artifact, unrelated to trading
graphify-out/
src/graphify-out/
